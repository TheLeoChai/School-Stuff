// JavaScript Document
var set = prompt("wat is the set number");
var question = $('.FormattedText.notranslate.TermText.StudentPrompt-promptText').getAttribute("aria-label");
var prevQuestion;
if(question !== prevQuestion){
	//question changed, search for ans;
	var corAns = ;//insert correct ans, search from data base
	
}